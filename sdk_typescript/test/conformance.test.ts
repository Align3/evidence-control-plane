import { test } from "node:test";
import "./run-vectors.ts";

test("ES-S-007 TypeScript canonical bytes and digests match the published cross-implementation corpus", () => {
  // Importing run-vectors executes all agreement and adversarial vectors.
});
