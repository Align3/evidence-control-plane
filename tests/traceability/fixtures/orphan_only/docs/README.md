# Fixture: the same requirement with no assertion catalogue

There is no `attestation-reliance.md` here, so no assertion finding is produced
and CM-401 is the only thing wrong: a specification requirement with no scenario
and no exemption, which is a medium finding. Used to show that a critical-only
gate does not trip on it, and that raising the threshold does.
