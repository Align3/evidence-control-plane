# Fixture: agent working agreement

> **Non-testable (document default) — meta.** Every AG-nnn requirement here
> governs how agents and reviewers work rather than how the system behaves.
> Individual requirements may override this with their own marker.

---

**AG-901** — A requirement inheriting the document default.

**AG-902** — A second requirement inheriting the document default.

**AG-903** — A requirement that overrides the document default with its own,
more specific reason.

> **Non-testable — process.** This one is enforced by the branch protection
> configuration rather than by the review protocol the default describes.
